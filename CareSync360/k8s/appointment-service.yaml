apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - name: http
      protocol: TCP
      port: 4003
      targetPort: 4003
  type: ClusterIP
